import 'package:flutter/material.dart';

class PreviewSet extends StatelessWidget {
  const PreviewSet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
